[build-system]
requires = ["maturin>=1.8.1"]
build-backend = "maturin"

[project]
name = "codeanatomy_engine"
version = "0.1.0"
description = "CodeAnatomy Rust engine PyO3 bindings"
requires-python = ">=3.13"

[tool.maturin]
module-name = "codeanatomy_engine"
locked = true
